extends ../layout
block content
  h1 #{article.title}
  h5 Written By #{author}
  p #{article.body}
  hr
  a.btn.btn-primary.mr-2(href="/article/"+article.id+"/edit") Edit
  a.btn.btn-danger.delete-article(href="#" data-id=article._id) Delete
